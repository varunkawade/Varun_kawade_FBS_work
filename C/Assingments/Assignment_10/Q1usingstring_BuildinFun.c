// str length
#include <stdio.h>
#include <string.h>

int main() {
    char str[] = "Varun Kawade";
    int length = strlen(str);
    printf("Length of string is : %d\n", length);
    return 0;
}
